// Canonical category order for the effects gallery and sidebar.
// Values must match the `category` field in effect registry entries exactly.
export const EFFECT_CATEGORIES = [
	'Text',
	'Scroll',
	'Cursor',
	'Background',
] as const;
